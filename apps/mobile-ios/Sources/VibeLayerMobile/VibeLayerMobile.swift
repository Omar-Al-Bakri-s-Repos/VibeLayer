import Foundation

public struct VibeLayerMobile {
    public static let version = "1.0.0"
    
    public static func initialize() {
        // Initialize VibeLayer mobile SDK
        print("VibeLayer Mobile iOS SDK v\(version) initialized")
    }
}
